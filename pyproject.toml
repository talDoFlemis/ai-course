[project]
name = "ai-course"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "debugpy>=1.8.8",
]

[dependency-groups]
dev = [
    "pytest>=8.3.3",
]
